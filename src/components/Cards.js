import React, { useEffect } from "react";
// const axios = require("axios");
// const qs = require("qs");
import axios from "axios";
import qs from "qs";
import { Card } from "./Card";

export const Cards = ({ leaders, ogs, members, pet, hangarounds }) => {
    return (
        <div>
            {/* {streamers.map((streamer) => (
                <Card
                    streamer={streamer.toString()}
                    key={streamer.toString()}
                />
            ))} */}
        </div>
    );
};
